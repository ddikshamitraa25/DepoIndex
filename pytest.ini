[pytest]
pythonpath = .
testpaths = tests
markers =
    requires_pdf: tests that require the private source deposition PDF
    requires_transcript: tests that require outputs/canonical_transcript.json
